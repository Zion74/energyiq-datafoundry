---
title: "2026-08-01 开发记录：Ngee Ann 源到事实契约原型"
summary: "用两份真实 Level 6 Excel 验证统一 Source Adapter、累计读数差分、冲突留痕、Virtual Load 12 与官方汇总排重。"
doc_type: runlog
tags: [Ngee Ann, Source Adapter, Interval Fact, Virtual Meter, 原型]
updated_at: "2026-08-01"
related:
  - "2026-07-31-可信查询范围与Energy-Fact接入记录.md"
  - "领域模型.md"
  - "开发计划-Admin与模板运行闭环.md"
status: completed
---

# 2026-08-01 开发记录：Ngee Ann 源到事实契约原型

## 1. 目标与范围

本轮只回答一个问题：**Excel 当前接入与 Tuya API 未来接入，能否在数据源之后共用同一套可追溯事实、质量、虚拟表和官方汇总逻辑？**

验证范围：

- 读取两份真实 Ngee Ann Level 6 Excel；
- Excel Source Binding 使用已确认的完整 `Device Name`；
- 累计读数转换为保留实际时长的 Interval Fact；
- 重叠批次确定性选取规范读数，同时保留冲突证据；
- 计算 `Virtual Load 12 = Load 1 + Load 2`；
- 证明 Virtual Meter 默认不进入官方总量；
- 用合成边缘场景运行 gap、reset、冲突和缺失输入规则。

本轮不实现数据库迁移、Admin 页面、正式 Tuya Connector、通用公式解析器或生产发布流程。

## 2. 原型改动

| 文件 | 类型 | 说明 |
| --- | --- | --- |
| `scripts/energyiq/prototypes/source-to-fact/contract.mjs` | 新增 | 纯函数形式的候选 Source-to-Fact 契约 |
| `scripts/energyiq/prototypes/source-to-fact/prototype.mjs` | 新增 | 读取真实 Excel、可交互查看完整状态的可丢弃终端外壳 |
| `scripts/energyiq/prototypes/source-to-fact/README.md` | 新增 | 问题、运行命令、关系图与原型边界 |
| `package.json` | 修改 | 增加单命令 `prototype:energy-source-facts` |

原型不写数据库，不修改 Excel，所有状态只存在于内存。

### 2.1 统一 Adapter 输出

Excel 与 Tuya 只负责产生同一种 Import Batch：

~~~text
Import Batch
├── batchId / sourceKind / sourceId
├── artifactSha256 / coverageEnd
└── Raw Reading[]
    ├── stableSourceKey
    ├── sourceLabel
    ├── observedAt / cumulativeValue / unit
    └── sourceRef
~~~

稳定来源键：

- Excel：`excel:<exact Device Name>`；
- Tuya：`tuya:<device_id>:<dp_code>`。

名称只用于 Excel 当前映射或用户展示；未来 Tuya 不能用可变设备名称充当稳定身份。

### 2.2 已验证的事实规则

1. 同一文件 SHA 重试直接返回 `duplicate_skipped`，不会产生第二个批次或重复事实；
2. 重叠批次同键同值直接去重；同键不同值保留全部 Source Candidate，并按 `coverageEnd` 较晚、再按 `batchId` 确定规范值；
3. 正常区间使用相邻累计值之差，平均功率使用实际区间时长；
4. gap 和短间隔保留实际时长及能量，但状态为 warning；它们可以进入日级总量，不能伪装成完整 15 分钟覆盖；
5. reset/负差无法可靠推导能量，`usageValue` 为空并标记 invalid；
6. Virtual Meter 按完全一致的区间键对齐输入；缺少任一输入时标记 `incomplete_inputs`，禁止按零补齐；
7. 官方汇总只读取显式 `officialAggregation=true` 的 Meter Point。`Virtual Load 12` 默认 analytical-only，不与 Total Office Load 重复相加。

第 4 条比当前 Python Builder 的“gap 不进入用量”更适合日级分析：有真实前后累计值时，区间总能量仍然成立，只是无法拆成完整的 15 分钟曲线。正式吸收时必须同步修改 Builder 与质量口径，不能让两套逻辑长期并存。

## 3. 验证证据

### 3.1 两份真实 Level 6 Excel

~~~powershell
npm run prototype:energy-source-facts -- --demo
~~~

结果：

| 指标 | 结果 |
| --- | ---: |
| Import Batch | 2 |
| Raw Reading | 51,838 |
| Canonical Reading | 50,111 |
| 去除同值重复 | 1,720 |
| 重叠冲突键 | 7 |
| Level 6 official load | 3,186.762 kWh |
| Level 6 official light | 1,006.799 kWh |
| Level 6 official all-category | 4,193.561 kWh |

冲突未阻断运行，但 Snapshot 状态为 `publishable_with_quality_warnings`，证据中保留批次、文件 SHA、规范值选择规则、Setup Revision 和 Formula Revision。

### 3.2 Virtual Load 12

~~~powershell
npm run prototype:energy-source-facts -- --demo --view=v
~~~

结果：

~~~text
Load 1:  59.960 kWh
Load 2: 336.620 kWh
Virtual Load 12: 396.579 kWh
Formula check:   396.579 kWh
Official Total Office Load: 3186.762 kWh
Incomplete virtual intervals: 0
~~~

这证明虚拟表可以与同 Scope 的物理表单独分析，但不影响上级官方总量。

### 3.3 边缘场景

~~~powershell
npm run prototype:energy-source-facts -- --demo --view=e
~~~

同一纯函数契约产生：

~~~text
boundary: 2
overlap_conflict: 4
gap: 2
cumulative_reset_or_negative_delta: 2
incomplete_inputs: 2
Virtual incomplete intervals: 2
~~~

SHA 幂等单独复验后，第一次为 `accepted`，第二次为 `duplicate_skipped`，最终 Batch 数仍为 1。

## 4. 问题与取舍

| 问题 | 处理 |
| --- | --- |
| Excel 时间含 `xx:14:59.999` 浮点误差 | Adapter 先按分钟取整，再按 Asia/Singapore 转 UTC |
| 两个导出文件覆盖时间重叠且少量值冲突 | 原始候选全部留存，规范值选择确定且可解释 |
| gap 是否直接丢弃能量 | 不丢弃；保留实际时长和总能量，覆盖率与 15 分钟曲线降级 |
| reset 后不知道真实用量 | 不猜测、不补零，生成 invalid Fact |
| 虚拟表是否参与官方汇总 | 默认不参与；只有未来显式配置为 official component 时才替换输入项 |

原型首次边缘场景运行曾因合成 Source Binding 缺少 `excel:` 前缀而没有生成 Fact；修正夹具后规则正常触发。正式实现应让 Mapping 发布校验阻止这类不匹配。

## 5. 本地复现

交互运行：

~~~powershell
npm run prototype:energy-source-facts
~~~

可用命令：

~~~text
[1] import Apr–May
[2] import overlapping May–Jun
[r] retry latest SHA
[o] overview
[v] virtual meter
[d] adapter/evidence
[e] edge cases
[q] quit
~~~

## 6. 后续

1. 将候选纯逻辑吸收到正式 Meter Point、Source Binding、Import Batch、Raw Reading、Interval Fact 和 Virtual Formula 模块；
2. 以数据库唯一键和事务实现 SHA 与事实键幂等；
3. 让当前 Python Builder 与正式契约只保留一套 gap/reset 质量语义；
4. 接着原型验证 Admin 的首次导入、Mapping、Virtual Meter、质量检查与 Setup 发布动线；
5. 获得正式 Tuya API 文档后，只实现新的 Adapter，不复制差分、质量或虚拟表逻辑。
