---
title: "2026-08-16 AI 输出审核边界二次验证结论与实施计划"
summary: "独立复核 Layer 1–3 与 A→B 真实代码后，采用 Evidence 约束事实、允许推断与猜想扩展、按最小片段局部失败的 Option 3+，并给出红测、版本旋转和验收顺序。"
doc_type: decision
tags: [AI审核, Evidence, Epistemic-Status, A-B, Stage-3]
updated_at: "2026-08-16"
related:
  - "2026-08-05-Overview用户价值与AI-Slot最小交付决策.md"
  - "2026-08-08-AI-Analyst-Harness与AI-Slot执行路径.md"
  - "开发计划-Admin与模板运行闭环.md"
status: provisional
---

# 2026-08-16 AI 输出审核边界二次验证结论与实施计划

> 本文是对侧边 Agent 讨论输入的独立二次验证结论。它固定后续红测和修改方向，但在真实 Provider、浏览器和人工价值验收完成前，不把产品效果标记为 accepted。

## 1. 验证基线与结论

二次验证使用：

- 讨论输入：隔离工作树 `energyiq-datafoundry-admin-readiness` 中的《AI 输出审核边界讨论与二次验证请求》，未把其中判断直接当作结论；
- 工作树：`D:\Projects\energyiq-datafoundry-demo-integration`；
- 分支：`codex/stage3-pass3-provider-hotfix`；
- 独立只读审查固定基线：`9cbb401`；后续 v19 修复仍是本工作树的待提交 WIP，不能与已提交基线或已生成 Provider Artifact 混写；
- 实现：Layer 2 Section Interpretation v4、Layer 1 Executive v4、Additional Insights WIP current identity v19（v18 及更早版本历史只读）；
- 真实 A→B 证据：A 与 B 均生成独立 Artifact，但旧 Transition 曾把不同 Findings 错判为 `no-material-change`。

**独立结论：部分同意讨论文档，选择 Option 3+。**

同意：

1. Evidence 应约束具体事实，不应要求 Evidence 直接证明所有推断、猜想、反例和低风险实验；
2. 运行时审核与 Stage 3 价值审核必须分工；机器门不负责决定内容是否有启发性；
3. 一个坏句子或 Candidate 应局部失败，不能吞掉有效 sibling、整个 Section 或确定性 Overview；
4. Layer 2 需要复用 Typed Evidence 的 Metric、Entity、Time、Value、Unit 绑定，不能继续只靠“同一个数字在 Evidence 中出现过”；
5. Layer 1 需要 Claim 级来源，不能只凭 Evidence ref 和关键词推断事实等级；
6. A→B 必须完整解释每个 A Finding 和每个 B Finding，不能把模型分类直接当成权威事实。

不同意或需要收窄：

1. **不直接采用任意长度的通用 `claims[]`。** 它会增加填表负担、迁移复杂度和页面机械感；
2. **不一次性删除所有关键词规则。** 关键词可降为 Prompt 提示、诊断或迁移 fallback；安全、Alert 和危险表达扫描仍是硬边界；
3. **不把错误事实降成 speculative 后保留。** 猜想可以没有直接原因 Evidence，但猜想中引用的历史数字、Centre、日期和设备事实仍必须真实；
4. **不为了保留创造性而接受无法拆分的错误前提。** 可独立的有价值角度应保留；依赖错误前提的结论需要 candidate-local repair 或拒绝；
5. **不把 pass@3 机器门变成第二个内容审稿模型。** 新颖性、启发性、清晰度和用户价值继续由盲评与人工批准判断；
6. **不强制每张卡同时填写 observation、angle 和 experiment。** 三者是可选表达通道，至少一个存在即可；纯事实新发现、纯探索角度和低风险实验都可以独立成为有价值的 Insight。

## 2. 当前事实表

| 层级 | 当前真实行为 | 判断 | 主要缺口 |
| --- | --- | --- | --- |
| Layer 2 Section | `materializePreschoolSectionResultV4` 对 Summary/Candidate 做 Evidence、数字、单位、Centre、日期、关系和安全检查；数字错误可按句 salvage | 方向正确 | `hasUnsupportedNumber` 仍先汇总被引 Evidence 的数值池；同值但错 Metric/Entity/Time 有误放风险 |
| Layer 2 Epistemic | `calibrateCandidateEpistemicStatus` 按 `may/might/could/recommend...` 把 observed 降为 inferred | 仅可作迁移 fallback | 正确语义可能因措辞误判；未命中词不代表是 observed |
| Layer 2 Failure | 数字错误有最小句子 salvage，其他错误多为 Candidate 局部拒绝 | 基本正确 | 混合事实/角度仍需更深的字段边界，避免依赖正则切句 |
| Layer 1 Key Findings | Provider 引用 Section/Evidence；坏 Finding 可局部丢弃 | 基本正确 | Artifact 没有精确 `sourceClaimIds`；同一 Evidence 同时支持 observed 与 inferred 时可能升级或误拒 |
| Layer 1 Epistemic | 通过 Evidence ref 聚合来源等级，并要求句子出现限定词 | 应替换 | 属于关键词监狱，且 Evidence ref 不是 Claim identity |
| Layer 3 Additional | 已分离 `observation` 与 `angle`，Typed Evidence 校验事实，支持局部 salvage、Method/Tool/Artifact provenance | 三层中最接近目标 | 当前仍要求 observation 与 angle 同时存在；Experiment 参数尚未结构化；部分 causal/action/benchmark 误标会吞掉整卡 |
| Stage 3 machine gate | 校验合同、事实 lineage、provenance、重复、长度和恢复完整性 | 职责正确 | `fact-boundary` 名称容易被理解为“所有想法都需直接证明”，实际应只约束事实与 lineage |
| Stage 3 blind review | Summary 与 Findings 分别评分，至少两次 machine pass 且两次人工 pass | 职责正确 | Review Pack 只有技术 Evidence ref，缺可读 label/Metric/value/unit/period/entity；人工难以实质判断 epistemic honesty |
| A→B Transition | 模型返回 New/Changed/Still supported/Resolved/No material change，服务端校验 Finding/Evidence identity | 原实现不完整 | 旧实现没有要求全集覆盖，真实运行把不同 A/B Finding 误判为无变化 |

## 3. 最终审核边界

### 3.1 运行时审核

必须负责：

- Workspace、Project、Scope、Snapshot、Release、Period、Model、Method、Tool 和 Artifact identity；
- `observation` 中 Metric、Entity、Time、Value、Unit 的直接 Evidence 绑定；
- 历史数字、日期、Centre、设备、Alert、Tool Audit 和租户边界；
- 不安全 Markdown/HTML、Prompt/SQL 泄漏和高风险操作；
- Candidate/句子级 failure isolation；
- 新 revision 只允许 current identity 写入，旧 revision 历史只读。

历史量化事实即使写成 `may have increased 999%` 也必须校验；只有明确的 forward-looking experiment/scenario parameter 才能不作为历史测量值处理。机器门中的 `fact-boundary` 应理解为 Evidence lineage 与事实边界，不能被宣传成“模型的全部推理已被证明”。

不负责：

- 判断一个合理猜想是否一定正确；
- 要求推断关系已作为确定性 Fact 存在；
- 仅按 `may/might/could` 决定内容价值；
- 以固定 What/Why/How 模板填满卡片。

### 3.2 内容类型

| 类型 | Evidence 要求 | 允许内容 | 禁止内容 |
| --- | --- | --- | --- |
| Observed | 必须直接证明完整事实 | 已发生的信号、数字、对象、时段 | 借用同值但错误 Metric/对象；未证实原因 |
| Inferred | 必须证明基础信号；关系可由 AI 推导 | 关系、反例、优先级、联系 | 把推导写成已确认事实 |
| Speculative | 至少一个当前 Snapshot Evidence 锚点 | 可能原因、What-if、值得追问的角度 | 伪造历史数字/事故/设备状态 |
| Experiment | Evidence 说明为什么值得尝试 | 低风险、可逆、短周期、带验证指标的尝试 | 把 proposed parameter 冒充历史事实；高风险自动执行 |
| Alert | 严格 Evidence 和 certainty 门 | 确认异常或明确 possible 提示 | 把漏水、短路、火灾等猜想冒充确认事故 |

### 3.3 Stage 3 质量审核

- 机器门：合同、事实、lineage、身份、来源、重复、恢复；
- 盲评：新角度、相关性、清晰度、值得深挖、诚实性、用户价值；
- 人工批准：决定是否成为 publication candidate 或 Method/SOP Proposal；
- 发布：必须绑定批准的 exact attempt，不得新增 Provider Run 或替换内容。

## 4. 方案比较

| 方案 | 做法 | 优点 | 主要风险 | 结论 |
| --- | --- | --- | --- | --- |
| A. 保持 Card + 关键词硬门 | 继续扩正则与专项补丁 | 改动最小 | 误伤、漏判、维护成本持续上升 | 不选 |
| B. 通用 `claims[]` | 所有内容拆成任意 Claim 列表 | 校验最细 | 模型填表、Schema 过重、前端机械化 | 暂不选 |
| C. 自由 Card + 只提取事实 Claim | 只对可识别事实做 Typed binding | 创造性限制最小 | 混合叙事的局部修复边界不稳定 | 可作过渡，不作最终接口 |
| D. Option 3+ | 自然 Card 内保留少量 `observation / angle / experiment` 通道；Key Findings 用 `sourceClaimIds` | 事实精确、角度开放、页面仍自然、可渐进迁移 | 需要合同旋转与历史兼容 | **选择** |

推荐形状：

```ts
{
  title: string;
  observation?: { text: string; evidenceRefs: string[] };
  angle?: {
    text: string;
    epistemicStatus: "inferred" | "speculative";
    anchorEvidenceRefs: string[];
  };
  experiment?: {
    text: string;
    anchorEvidenceRefs: string[];
    parameters: Array<{ value: number; unit: string }>;
  };
}
```

底层结构化不要求前端显示为表格。前端继续呈现自然 Card，并用 `Evidence signal / AI angle / Try this`、epistemic badge、Markdown 强调和 Evidence 下钻组织内容。

Layer 1 不复用 Card 自报的 Evidence 聚合，而使用：

```ts
{
  title: string;
  text: string;
  sourceClaimIds: string[];
  alert?: ...;
}
```

服务端从 exact Section Claim 派生 Section、Evidence 和最终 epistemic status。

## 5. 已确认的 A→B 紧急修复

真实 A/B Transition 暴露了一个独立于“文字是否有价值”的正确性缺口：A 和 B 的 Findings、Evidence、对象均不同，模型却返回 `no-material-change`，旧服务端仍将其保存为 completed。

本分支已按公开 `executeTransition` seam 完成 RED→GREEN：

- 非空 A/B 不允许 `no-material-change`；
- 每个 A Finding 和每个 B Finding 必须恰好被分类一次；
- 重复使用同一 Finding、遗漏其他 Finding 时失败；
- A 消失而 B 为空时允许 `Resolved`；
- A/B 均为空时允许 `No material change`；
- 无关的非空 A/B 应分别返回 `Resolved` 与 `New`，不能隐藏变化；
- 失败归类为 `validation`，保留旧 Transition 作为不可变审计，不改写历史。

自动化证据：Evaluation、Additional Workflow、Metadata Store、API 与公共 Contracts 共 5 个文件 110/110 通过；其中 `preschool-additional-ai-insights-evaluation.test.ts` 26/26 通过；API TypeScript 构建通过。真实 Provider 已用新 idempotency key 复跑：A 有一条批准 Finding、B 为 honest empty，Transition 恰好返回一条 `resolved`；Run 总数 610→612，只有两次明确授权的 Transition Provider 调用。

## 6. RED 测试计划

所有测试必须打公开 production seam，不直接调用私有 regex helper。

### 6.1 Layer 2 Typed Fact Authority

入口：`materializePreschoolSectionResultV4`。

1. Evidence 中存在 `12% total energy`，Claim 写 `12% peak change` → 局部拒绝；
2. 数值相同但 Centre、Metric 或 Period 不同 → 局部拒绝；
3. 数值、Metric、Centre、Period、Unit 全正确 → 接受 observation；
4. 多 Evidence 提出新关系且不含 `may` → 接受并派生 inferred；
5. manual override / cleaning activity 无直接原因 Evidence → 作为 speculative angle 保留；
6. `Centre L 可能用了 7.8 kWh` 且 7.8 不存在 → 拒绝历史事实；
7. `提前 30 分钟关闭` 明确为 experiment parameter → 接受，不要求历史 Evidence 有 30；
8. 错误事实句与独立有价值假设句并存 → 删除错误句、保留假设；
9. 假设依赖错误前提 → 不自动 salvage，candidate-local repair 或拒绝；
10. 一个 Candidate 错误 → 其他 Candidate、Section 和确定性 Overview 不受影响。

### 6.2 Layer 1 精确来源

入口：`createPreschoolExecutiveSynthesizer(...).execute`。

1. 同一 Evidence 同时被 observed 与 inferred Section Claim 使用 → Key Finding 按 `sourceClaimIds` 精确继承，不升级；
2. inferred 关系没有命中 `may/might` → 不仅因措辞拒绝；
3. 一句前半段有 `may`、后半段断言确定原因 → 不得借一个限定词绕过；
4. Cross-Section tool 两个信号 → 可形成 inferred connection；
5. 未引用相关 Section/Tool Claim 的跨 Section 实体 → 局部拒绝；
6. 一个坏 Finding → Summary 和 sibling Findings 保留。

### 6.3 Layer 3 与 A→B

入口：`evaluateAttempt`、`executeTransition`。

1. causal/action 误标 observed，但 observation 与 angle 可分离 → 保留 observation，angle 降级或 repair；
2. B 的 experiment 参数恰好等于 A 历史事实数字 → 不误判 Snapshot 泄漏；
3. B 复述 A 历史数字但 B Evidence 不支持 → 比较前失败；
4. `no-material-change` 但 B 内容/Evidence/epistemic status 改变 → 拒绝；
5. `still-supported` 的 A/B Metric/Entity 不兼容 → 拒绝或要求 Changed/New+Resolved；
6. A/B Findings 全集未恰好覆盖 → 失败；
7. `may have increased 999% in the previous period` 且无 comparison Evidence → 删除该历史量化句或局部拒绝，不能借 modal 绕过；
8. `may be 999% higher than last month, a scenario worth investigating`，以及在历史断言后附加 `and a proposed schedule adjustment...` → 仍按历史量化断言校验；实验标签或邻近共现不能洗白旧数字；
9. `might be 10% lower after schedule changes` 明确作为未来场景参数 → 可保留，不冒充历史事实；
10. `Peak demand and total energy use rose 4.63%...` → 两个指标必须分别拥有同周期 comparison Evidence；共享谓语不能借用一个指标的 Evidence；
11. `Centre G and Centre H energy use increased...` → 每个 Centre 都必须通过候选自身 Evidence 与比较边界，坏句局部删除，卡片中仍受支持的探索角度保留；
9. 纯 observation-only、angle-only 或 experiment-only Candidate → 在其唯一通道合法且有价值时可发布；
10. 普通 Overview、Saved A、刷新 → Provider Run 数不增加。

### 6.4 UI

入口：`PreschoolAiSlot`、`OverviewChangeDialog`。

1. Key Finding inferred/speculative → 显示 badge，不强迫正文写固定限定词；
2. A/B 显示具体 `A title → B title`、分类和 Evidence basis，不只显示数量；
3. B AI 未完成 → 确定性 Overview 和指标比较先显示；
4. AI 恢复完成 → 内容原位更新，不推开主要图表或制造页面跳动；
5. Evidence 首层可读，技术 ID 放二级详情。

## 7. 修改切片与依赖顺序

### Slice 0 — A→B 完整性热修（当前）

- Owned：`preschool-additional-ai-insights-evaluation.ts/.test.ts`；
- 自动门：26 个 Evaluation tests、相关 Contracts/Metadata/API tests、API build；
- 之后重建 API，以新 idempotency key 做一次真实 Provider Transition。

### Slice 1 — Layer 2 Typed Fact Authority

- 先让 Section Pack 暴露服务端生成的 typed claim facts；
- 复用 `energyAiNarrativeClaimsSupported`，补齐 Metric/Entity/Time 语义；
- 将自然 Card 拆成 observation/angle/experiment 最小通道；
- 不改 Layer 1/3 文件。

### Slice 2 — Layer 1 `sourceClaimIds`

- Executive Provider 只选择来源 Claim；
- 服务端派生 Section、Evidence、epistemic status；
- 删除“每句必须命中限定词”的硬门，保留 Prompt 提示与诊断；
- Web 显示 Key Finding status。

### Slice 3 — Layer 3 experiment 与局部降级

- 保留现有 observation/angle 和 Method/Tool governance；
- 增加 typed experiment parameters；
- 对可分离的错误事实做最小删除/repair，不让关键词吞掉整卡；
- Alert、外部 benchmark 和高风险动作继续 fail closed。

### Slice 4 — A→B 客户表达与真实验收

- 基于已验证 Claim 比较，不扫描无类型的全部数字；
- UI 展示具体保留、变化、新增、解决及 Evidence basis；
- 真实 A→B 后重新人工判断内容价值，不把“分类完成”当成有用。

共享冲突面：`preschool-overview-ai-structured-output.ts`、`overview-ai-artifact.ts` 和公共 Contracts。Slice 1–3 对这些文件必须串行；RED 设计、只读审查、浏览器矩阵和部署准备可以并行。

## 8. 身份与历史兼容

若实施合同变化：

- Layer 2：Section output/identity 升到 v5，Pack 升到 v3；v4 历史只读；
- Layer 1：Executive output/identity 升到 v5；v4 历史只读；
- Layer 3：Candidate 局部修复、事实标题边界及 provenance 语义先旋转到 v18；随后把确定性 superlative 绑定到同一 Centre、同一指标和同一排名方向，并堵住 modal、后置 scenario 标签、跨指标共享谓语对历史量化事实的绕过，形成 v19 / validator v16 / workflow v19。真实 v19 pass@3 又证明 DeepSeek 在非截断情况下会产生“尾部多一个括号”或“属性开引号和根尾括号各缺一个”的外壳标点损坏。系统因此只增加 root-anchored、最多四处、仅属性开引号与尾部括号平衡的确定性修复，修复后仍要求 exact `{candidates}` 并逐候选校验；current identity 旋转为 v20、validator 保持 v16、workflow 升为 v20，v19 及更早版本历史只读；若未来持久化 typed experiment 字段或改用 submission tool，仍需再次旋转 output/identity；
- queue/claim/renew/complete/fail 只接受 current identity；历史 terminal 只读；历史 running fail closed；
- 严格数据归因 A→B 只比较同一 Prompt/Workflow/Validator/Method/Profile 基础。版本旋转后先建立新 A baseline；旧版 A 与新版 B 只能做 UI 内容对照，并明确不能把差异全部归因于数据更新。

Transition-only Slice 没有旋转 Additional Artifact；后续 Candidate acceptance 与事实排名绑定改变了可发布集合，因此已单独旋转 Additional Artifact identity，旧 Artifact 不会被当成 current 复用。

## 9. 验收门与停止线

依次完成：

1. 每个新增行为先有公开 seam RED；
2. targeted Vitest；
3. `contracts → metadata → API → Web` 顺序构建，避免 stale `dist`；
4. `npm run test:energyiq:seams`；
5. API/Web production build；
6. 真实 Provider 三次独立 attempt，至少两次 machine pass、两次人工通过；
7. 真实 A→B：旧结论保留/变化/淘汰，新异常可出现，旧 Artifact 不冒充当前结果；
8. 普通打开、刷新、Saved A 恢复前后 Run 数不增加；
9. 浏览器检查 Layer 1–3 的重点、自然度、长度、Evidence 下钻和移动端；
10. 多账户检查 Charles admin、Ngee-only user、Overview 与 AI Analysis 数据域隔离；
11. 部署前后重复健康、数据库、权限、Run-count 和回滚门。

停止线：没有真实 Provider、浏览器和人工价值证据时，只能声明代码/合同门通过，不能声明 AI 内容效果或生产部署验收完成。

## 10. 2026-08-16 真实验收记录

### 10.1 Stage 3 Provider、人工批准与发布

- v18 pass@3 evaluation：`additional-evaluation-b3e4fe14-5ef3-4ff9-8589-9d81d7349dba`（这是已完成的历史质量证据；v19 发布前仍需重新生成 current Artifact）；
- 三次独立 Provider attempt 全部 completed，零 failed；其中两份进入 blind review；
- 两份均通过人工价值门，选择并批准更有启发性的 contrarian angle：60 个 opening-hour spikes 覆盖 28/30 Centres，但 closed-hour peaks 只覆盖 4 个 Centres，因此不应把所有 opening spikes 直接当作设备缺陷；
- 正式发布 Artifact：`overview-ai-artifact-3a1de639c75b72450a012b2e`，exact v18 / validator v15 / workflow v18；
- v18 发布后本地 Overview 能恢复同一 Additional Insight；普通刷新和打开 A/B 弹窗前后 Run 总数保持 621，页面没有启动 Provider；v19 上线后必须由管理员显式重新生成，普通 Overview 读取不得代替生成；
- 内容仍有一个非阻断语言问题：标题中的 `fully-vetted 4` 不够自然；核心 Evidence 与 AI angle 清楚且有价值，后续通过质量反馈修正文风，不为此再旋转一次身份。
- v19 真实 pass@3 evaluation：`additional-evaluation-5cef0c2a-3265-405d-b23d-c049dc06a44e`。三次 Provider Run 均完成，Run 总数 621→624，但三份终态文本分别出现多余根括号、scratch/DSML 无最终 JSON、缺属性开引号并缺根尾括号，因此三次 attempt 均以 `PRESCHOOL_ADDITIONAL_AI_DISCOVERY_RESULT_INVALID` 在 structured-output 边界失败；这不是 API key、数据库、context 或 output-token 上限问题。
- v20 的 bounded repair 已用公开 Workflow seam 复现并覆盖上述两种可安全修复的标点损坏；scratch/前言、Markdown fence、相邻双对象、超过修复预算和错误根键继续拒绝，字段、数字与 Evidence 不改写，坏候选仍局部拒绝。自动门为 8 files / 141 tests 通过；真实 v20 pass@3、人工盲评和发布仍必须重新执行，不能复用 v18/v19 结果冒充 current。

### 10.2 A→B 与 Saved Overview

- 后端真实 A→B 已通过：每个旧 Finding 被保留、变化或淘汰；本次 A 的一条 Finding 在 B honest empty 后被准确标为 `resolved`，旧 Artifact 没有冒充 B；
- 客户可见的 `What changed?` 弹窗曾被一条历史损坏的 Saved AI payload 整体卡死；已按公开 UI seam 补 RED，并只跳过明确的 `ENERGYIQ_SAVED_ANALYSIS_AI_RESULT_INVALID` legacy record；网络、权限和未知读取错误仍 fail closed；
- 修复后的真实页面不再报错。当前没有符合“相同口径、相同窗口长度、严格早于 B”的 Saved A，因此诚实显示 `No compatible previous Overview is saved yet`；这不是转换失败。Charles 需要在每次有意义的数据更新后先保存 A，下一次 Snapshot 才会出现完整指标与 AI 结论变化；
- 当前客户端最多扫描最近 40 条 Saved Overview；正式服务端 previous-version endpoint 仍是非阻断后续项。

### 10.3 Layer 1–3 页面价值与视觉

- Section 2 Summary 已使用容器完整宽度，用户此前截图中的右侧空白已消失；粗体只强调 `Priority Centres G, M, J`，不是整段加粗；
- Layer 1 Key Findings 显示 `Reviewed 4 of 4 Sections`，三条结论能快速说明 operating-hour spikes、closed-hour share 和 G/M/J intensity screening；
- Layer 2 的 Section 2–4 Summary 与 Insights 能区分事实、推断和可能性；Section 5 在 B Snapshot 缺少 planning baseline 时局部 unavailable，确定性 Overview、其他 Sections 与 Additional Insight 不受影响；
- Layer 3 Card 使用 `Evidence signal / AI angle` 分工和 epistemic badge，保留发散性推论但不允许伪造数字、Centre、日期或设备状态；
- 视觉组织已达到当前可用门：清晰卡片、Markdown 强调、Evidence 折叠、图表与 AI 内容相邻。动态图表由自由画板生成仍属于后续 Stage 4，当前不让模型返回任意 HTML/JS。

### 10.4 多账户与部署差异

- `energyiq.top` 只读登录验收：EnergyIQ Admin Demo 可见 Ngee Ann 与 Preschool；Ngee Ann Client 01 只见 Ngee Ann；Preschool Manager Demo 只见 Preschool；三者各自允许的 `current-overview-28d` AI Analysis context 均成功解析；
- 两个普通账户访问对方项目均未返回数据，但 API 当前返回 500，而不是更恰当的 403/404。租户隔离成立，错误语义需作为独立小修；
- 本地共享测试库只有 Admin 最终密码与账号文件同步，两个普通账号本地登录为 401；线上最终账号均成功。本地 seed 漂移不能冒充线上权限失败；
- 线上 Preschool 当前仍指向旧 Snapshot `energy-snapshot-563f8939fd90dc2fbef0018a`，本地验收 B 为 `energy-snapshot-b22d0eabc6be53e809a168d2`。代码发布与本地测试数据/Artifact 发布是两件事，不能声称线上已出现本地 v18 内容；v19 current Artifact 也必须在目标环境独立生成并审核；
- 六月 B Snapshot 的确定性总成本已正常计算为 S$6,676.67，费率边界没有阻断整体 Overview；按 Centre type 的 estimated cost 仍显示 unavailable，属于拆分归因缺口，不应伪造分项成本。

## 11. 失效条件

出现下列信号时复审 Option 3+：

- 模型长期无法稳定填写三个轻量通道；
- Candidate repair 成本高于重新生成；
- 新项目需要更复杂的 Claim 图而不是线性 Card；
- 实验建议进入自动执行或高风险控制；
- 客户要求完整审计每一个复合 Claim；
- Ngee Ann 第二项目证明 Preschool 的字段边界不可复用。
