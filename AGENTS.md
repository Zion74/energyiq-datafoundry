## Agent skills

### Issue tracker

Issues, specs, and development tickets live in GitHub Issues for `Zion74/energyiq-datafoundry`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical GitHub triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Use a single product context: the root `CONTEXT.md` is the entry point, `docs/energyiq/CONTEXT.md` is the canonical glossary, and `docs/energyiq/` holds architecture, decisions, and implementation records. See `docs/agents/domain.md`.

### EnergyIQ execution baseline

Use the Integration/Worker Worktree lifecycle and three high-level test Seams in `docs/agents/energyiq-execution-baseline.md`.
